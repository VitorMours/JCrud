package com.backend.jcrud.configs;

import org.springframework.context.annotation.Configuration;

/**
 * Placeholder configuration removed Springfox usage.
 * Springdoc starter provides auto-configuration for OpenAPI/Swagger UI.
 */
@Configuration
public class SwaggerConfig {
    // No custom configuration needed when using springdoc starter.
}
